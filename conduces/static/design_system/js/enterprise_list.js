document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-enterprise-list]').forEach((page)=>{const table=page.querySelector('table');if(!table)return;const search=page.querySelector('[data-el-search]');const density=page.querySelector('[data-el-density]');const panel=page.querySelector('[data-el-column-panel]');const options=page.querySelector('[data-el-column-options]');const headers=[...table.querySelectorAll('thead th')];const rows=[...table.querySelectorAll('tbody tr')];headers.forEach((th,i)=>{rows.forEach(row=>{const cell=row.children[i];if(cell&&!cell.dataset.label)cell.dataset.label=th.textContent.trim()})});if(search)search.addEventListener('input',()=>{const q=search.value.trim().toLocaleLowerCase();let visible=0;rows.forEach(row=>{const show=!q||row.textContent.toLocaleLowerCase().includes(q);row.hidden=!show;if(show)visible++});let empty=table.querySelector('.el-no-results');if(!visible&&q&&!empty){empty=document.createElement('tr');empty.className='el-no-results';empty.innerHTML=`<td colspan="${Math.max(headers.length,1)}">No encontramos resultados con estos filtros.</td>`;table.tBodies[0].append(empty)}else if(empty&&(visible||!q))empty.remove()});if(density)density.addEventListener('change',()=>{page.classList.toggle('el-density-compact',density.value==='compact');sessionStorage.setItem('el-density',density.value)});const saved=sessionStorage.getItem('el-density');if(saved&&density){density.value=saved;density.dispatchEvent(new Event('change'))}if(options)headers.forEach((th,i)=>{if(i===0||i===headers.length-1)return;const label=document.createElement('label');label.innerHTML=`<input type="checkbox" checked> ${th.textContent.trim()}`;label.querySelector('input').addEventListener('change',e=>{th.hidden=!e.target.checked;rows.forEach(r=>{if(r.children[i])r.children[i].hidden=!e.target.checked})});options.append(label)});page.querySelector('[data-el-columns]')?.addEventListener('click',()=>{panel.hidden=!panel.hidden});page.querySelector('[data-el-restore]')?.addEventListener('click',()=>options.querySelectorAll('input').forEach(x=>{if(!x.checked)x.click()}));page.querySelector('[data-el-refresh]')?.addEventListener('click',()=>location.reload());});});
