# Iconografía

Familia única SEDL Outline: viewBox 24, stroke 2, tamaños 16/20/24/32 y `currentColor`. El manifest y el registro Python comparten nombres. Iconos informativos requieren nombre accesible; los decorativos, `aria-hidden`. Fallback: `info`.
