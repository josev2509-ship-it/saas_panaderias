# Catálogo técnico

Ruta: `/_sedl/catalog/`. Requiere autenticación y usuario staff. `SEDL_CATALOG_ENABLED` permite desactivarlo; por defecto replica `DEBUG`. No usa el layout productivo ni consulta datos operativos.
