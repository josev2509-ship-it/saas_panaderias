# Datos demo

QA visual ejecutada sobre SQLite temporal aislada, nunca sobre `db.sqlite3`. Tenant: `SASTRE QA Demo`; todos los módulos relevantes activos. `generar_demo_ready` se ejecutó completo e idempotente.

Los datos P2P y administrativos fueron poblados. O2C full conserva su limitación conocida y no recibe datos inventados.
