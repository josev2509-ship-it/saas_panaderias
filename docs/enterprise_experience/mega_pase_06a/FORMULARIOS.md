# Formularios Enterprise

El patrón usa encabezado, `ds-form-section`, grilla de dos columnas, ayuda y errores inline, y `ds-sticky-actions`. En móvil la grilla pasa a una columna. Las acciones disponibles siguen siendo responsabilidad de cada flujo y sus permisos; el Design System solo las presenta.
