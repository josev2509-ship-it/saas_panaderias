# Decisiones

`CategoriaProveedor` es modelo jerárquico por empresa. `MonedaEmpresa`,
`CondicionPago`, `Impuesto` y `UnidadMedida` se reutilizan. Business Partner,
órdenes, recepciones, CxP, pagos, stock y evaluación automática quedan fuera.
