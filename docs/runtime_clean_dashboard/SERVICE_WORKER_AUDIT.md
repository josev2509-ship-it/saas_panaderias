# Auditoría Service Worker / PWA

No existen registros de `navigator.serviceWorker`, `service-worker.js`, `sw.js`, Cache API ni manifest PWA en el código operativo. `design_system/icons/manifest.json` es un catálogo de iconos, no un manifest PWA.

No hay workers, caché offline ni claves PWA que invalidar.
